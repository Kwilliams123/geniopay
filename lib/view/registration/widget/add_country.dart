import 'package:flutter/material.dart';

class AddCountry extends StatelessWidget {
  final VoidCallback onAddTap;
  const AddCountry({required this.onAddTap,
    Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
